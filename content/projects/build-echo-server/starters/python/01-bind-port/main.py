import socket


def main():
    # TODO: Create a TCP socket
    # TODO: Bind the socket to 127.0.0.1 on port 4221
    # TODO: Start listening for connections
    # TODO: Keep the server running (don't let it exit)
    #
    # Hints:
    #   - Use socket.socket(socket.AF_INET, socket.SOCK_STREAM) to create a TCP socket
    #   - Use .bind((host, port)) to bind to an address
    #   - Use .listen() to start listening
    #   - The server needs to stay running — a simple way is to call .accept()
    #     which blocks until a connection arrives
    pass


if __name__ == "__main__":
    main()
